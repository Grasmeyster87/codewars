<div class="markdown prose max-w-none mb-8" id="description"><p>Complete the solution so that it splits the string into strings of two characters in a list/array (depending on the language you use). If the string contains an odd number of characters then it should replace the missing second character of the final pair with an underscore ('_').</p>
<p>Examples:</p>
<pre><code>* 'abc' =&gt;  ['ab', 'c_']
* 'abcdef' =&gt; ['ab', 'cd', 'ef']
</code></pre>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-none mb-8" id="description"><p>Доповніть розв'язок так, щоб він розділив рядок на рядки з двох символів у списку/масиві (залежно від мови, яку ви використовуєте). Якщо рядок містить непарну кількість символів, то слід замінити відсутній другий символ останньої пари символом підкреслення ('_').</p>
<p>Приклади:</p>
<pre><code>* 'abc' => ['ab', 'c_']
* 'abcdef' => ['ab', 'cd', 'ef']
</code></pre>
</div>

Хороший вопрос! Давай разберём пошагово, что делает эта функция:

```js
function solution(str) {
    return (str + '_').match(/.{2}/g) || [];
}
```

### 🔍 Пошаговое объяснение

1. **`str + '_'`**  
   К исходной строке добавляется символ подчёркивания `_`.  
   Это нужно для того, чтобы гарантировать, что длина строки будет чётной. Если строка имеет нечётную длину, добавленный `_` станет "заполнителем".

   👉 Пример:  
   - `"abc"` → `"abc_"`  
   - `"abcd"` → `"abcd_"`

2. **`.match(/.{2}/g)`**  
   Регулярное выражение `/.{2}/g` ищет все последовательности из **двух символов подряд**.  
   Флаг `g` означает "глобальный поиск" — то есть вернёт массив всех совпадений.

   👉 Пример:  
   - `"abc_"` → `["ab", "c_"]`  
   - `"abcd_"` → `["ab", "cd"]`

3. **`|| []`**  
   Если `match` не найдёт совпадений (например, строка пустая), оно вернёт `null`.  
   Чтобы вместо `null` вернуть пустой массив, используется оператор `|| []`.

   👉 Пример:  
   - `""` → `"_"` → `["__"]` (на самом деле будет `["_"]`, потому что `_` + `_` не добавляется)  
   - но если бы совпадений не было, вернётся `[]`.

---

### 📦 Итог
Функция разбивает строку на массив подстрок длиной **по два символа**.  
Если длина строки нечётная, последний символ дополняется `_`.

### ✨ Примеры работы

```js
solution("abc");   // ["ab", "c_"]
solution("abcd");  // ["ab", "cd"]
solution("a");     // ["a_"]
solution("");      // ["_"]
```

---

Хочешь, я покажу, как можно переписать эту функцию более явно, без регулярных выражений, чтобы было проще понять логику?
