<div class="markdown prose max-w-none mb-8" id="description"><p>You will be given an array <code>a</code> and a value <code>x</code>. All you need to do is check whether the provided array contains the value.</p>
<p><code>a</code> can contain numbers or strings. <code>x</code> can be either.</p>
<p>Return <code>true</code> if the array contains the value, <code>false</code> if not. </p>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-none mb-8" id="description"><p>Вам будет предоставлен массив <code>a</code> и значение <code>x</code>. Все, что вам нужно сделать, это проверить, содержит ли предоставленный массив это значение.</p>
<p><code>a</code> может содержать числа или строки. <code>x</code> может быть и тем, и другим.</p>
<p>Возвращает <code>true</code>, если массив содержит значение, <code>false</code>, если нет.</p>
</div>