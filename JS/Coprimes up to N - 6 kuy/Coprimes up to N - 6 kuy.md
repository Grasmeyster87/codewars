Two integers are coprimes if the their only greatest common divisor is 1.
Task

In this kata you'll be given a number n >= 2 and output 
a list with all positive integers less than gcd(n, k) == 1, with k being any of the output numbers.

The list cannot include duplicated entries and has to be sorted.

Два целых числа взаимно просты, если их единственный наибольший общий делитель равен 1.
Задание

В этом задании вам будет задано число n ≥ 2, и выведен список всех положительных целых чисел, меньших НОД(n, k) == 1, где k — любое из выходных чисел.

Список не может содержать повторяющихся элементов и должен быть отсортирован.

Ты почти на правильном пути, но есть важное уточнение: **остаток от деления (`n % k`) — это не то, что нужно проверять**. Вместо этого нужно использовать **функцию НОД (наибольший общий делитель)** — `gcd(n, k)`.

---

## 🔍 Почему `n % k == 0` — недостаточно

Проверка `n % k == 0` говорит только о том, **делится ли \( n \) на \( k \)**. Но это **не определяет**, являются ли числа **взаимно простыми**.

Например:
- \( n = 10 \), \( k = 3 \)
  - \( 10 \% 3 = 1 \) → есть остаток
  - \( \text{gcd}(10, 3) = 1 \) → ✅ взаимно просты
- \( n = 10 \), \( k = 4 \)
  - \( 10 \% 4 = 2 \) → есть остаток
  - \( \text{gcd}(10, 4) = 2 \) → ❌ не взаимно просты

То есть наличие остатка **не гарантирует**, что числа взаимно просты.

---

## ✅ Что нужно делать

Вот правильный алгоритм:

```js
function coprimes(n) {
  const result = [];
  for (let k = 1; k < n; k++) {
    if (gcd(n, k) === 1) {
      result.push(k);
    }
  }
  return result;
}

// Функция для вычисления НОД (алгоритм Евклида) Function for calculating the greatest common divisor (Euclidean algorithm)
function gcd(a, b) {
  while (b !== 0) {
    [a, b] = [b, a % b];
  }
  return a;
}
```

---

## 📌 Вывод

- Перебираем все \( k \) от 1 до \( n - 1 \)
- Проверяем: `gcd(n, k) === 1`
- Если да — добавляем в результат

Хочешь, я помогу тебе протестировать этот код или объяснить, как работает `gcd`?

Отличный вопрос! Давай разберём, **как работает алгоритм Евклида** в этой функции `gcd(a, b)` — шаг за шагом.

---

## 📘 Что делает функция

Она находит **наибольший общий делитель (НОД)** двух чисел `a` и `b`.

---

## 🔧 Как работает алгоритм Евклида

Он основан на простом принципе:

> НОД(a, b) = НОД(b, a % b)

И повторяется, пока `b` не станет равным 0. Тогда `a` — это и есть НОД.

---

## 🔄 Пошаговое объяснение

Вот как работает эта строка:

```js
[a, b] = [b, a % b];
```

Это означает:
- `a` становится равным `b`
- `b` становится равным остатку от деления `a` на `b`

---

## 🧮 Пример: `gcd(48, 18)`

```js
a = 48, b = 18
→ [a, b] = [18, 48 % 18] → [18, 12]
→ [a, b] = [12, 18 % 12] → [12, 6]
→ [a, b] = [6, 12 % 6] → [6, 0]
→ b === 0 → return a = 6
```

Результат: `gcd(48, 18) = 6`

---

## ✅ Итог

Функция `gcd(a, b)`:
- Использует **алгоритм Евклида**
- Повторяет шаги, пока `b !== 0`
- Возвращает `a`, когда `b` становится 0 — это и есть НОД

Если хочешь, могу показать, как это работает визуально или на других примерах!
