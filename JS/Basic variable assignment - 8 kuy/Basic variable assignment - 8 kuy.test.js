const { assert } = require("chai")
const name = require('./Basic variable assignment - 8 kuy'); 


describe("Basic tests", () => {
  it("Testing for fixed tests", () => {
    assert.strictEqual(name, "codewa.rs");
    })
  })