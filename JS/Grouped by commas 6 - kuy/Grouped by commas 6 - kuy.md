Finish the solution so that it takes an input n (integer) and returns a string that is the decimal representation of the number grouped by commas after every 3 digits.

Assume: 0 <= n <= 2147483647
Examples

       1  ->           "1"
      10  ->          "10"
     100  ->         "100"
    1000  ->       "1,000"
   10000  ->      "10,000"
  100000  ->     "100,000"
 1000000  ->   "1,000,000"
35235235  ->  "35,235,235"


Завершите решение так, чтобы на вход подано n (целое число) и возвращалась строка, представляющая собой десятичное представление числа, сгруппированного запятыми после каждых 3 цифр.

Предположим: 0 <= n <= 2147483647
Примеры

       1  ->           "1"
      10  ->          "10"
     100  ->         "100"
    1000  ->       "1,000"
   10000  ->      "10,000"
  100000  ->     "100,000"
 1000000  ->   "1,000,000"
35235235  ->  "35,235,235"

Можно реализовать вставку запятых через `map` и `join`, но `map` сам по себе не знает о позиции в массиве относительно конца строки. Поэтому сначала нужно перевернуть массив, чтобы считать с конца, а потом использовать `map` с индексом:

### ✅ Вариант через `map` и `join`:
```js
function groupByCommas(n) {
  return n
    .toString()
    .split('')
    .reverse()
    .map((digit, i) => (i % 3 === 0 && i !== 0 ? digit + ',' : digit))
    .reverse()
    .join('');
}
```

### 📌 Пример:
```js
groupByCommas(123456789); // "123,456,789"
```

### 🧠 Как это работает:
- `split('')` — разбивает число на массив цифр.
- `reverse()` — чтобы считать позиции с конца.
- `map()` — добавляет запятую после каждой третьей цифры (кроме первой).
- `reverse()` — возвращаем порядок.
- `join('')` — объединяем всё в строку.

Хочешь сравнить с вариантом через `reduce` или регулярное выражение?
