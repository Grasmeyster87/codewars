<div class="markdown prose max-w-none mb-8" id="description"><h2 id="fix-the-function">Fix the function</h2>
<p>I created this function to add five to any number that was passed in to it and return the new value.
It doesn't throw any errors but it returns the wrong number.</p>
<p>Can you help me fix the function?</p>
</div>

<br><hr><br>

<div class="markdown prose max-w-none mb-8" id="description"><h2 id="fix-the-function">Виправити функцію</h2>
<p>Я створив цю функцію, щоб додавати п'ять до будь-якого числа, яке їй передано, та повертати нове значення.
Вона не видає жодних помилок, але повертає неправильне число.</p>
<p>Чи можете ви допомогти мені виправити функцію?</p>
</div>