<div class="markdown prose max-w-none mb-8" id="description"><p>Write a function that when given a URL as a string, parses out just the domain name and returns it as a string. For example:</p>
<pre><code>* url = "http://github.com/carbonfive/raygun" -&gt; domain name = "github"
* url = "http://www.zombie-bites.com"         -&gt; domain name = "zombie-bites"
* url = "https://www.cnet.com"                -&gt; domain name = cnet"
</code></pre>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-none mb-8" id="description"><p>Напишите функцию, которая, получив URL-адрес в виде строки, извлекает только доменное имя и возвращает его в виде строки. Например:</p>
<pre><code>* url = "http://github.com/carbonfive/raygun" -> domain name = "github"
* url = "http://www.zombie-bites.com" -> domain name = "zombie-bites"
* url = "https://www.cnet.com" -> domain name = cnet"
</code></pre>
</div>