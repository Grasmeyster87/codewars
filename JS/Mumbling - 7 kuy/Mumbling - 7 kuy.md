<div class="markdown prose max-w-none mb-8" id="description"><p>This time no story, no theory. The examples below show you how to write function <code>accum</code>:</p>
<h4 id="examples">Examples:</h4>
<pre><code>accum("abcd") -&gt; "A-Bb-Ccc-Dddd"
accum("RqaEzty") -&gt; "R-Qq-Aaa-Eeee-Zzzzz-Tttttt-Yyyyyyy"
accum("cwAt") -&gt; "C-Ww-Aaa-Tttt"
</code></pre>
<p>The parameter of accum is a string which includes only letters from <code>a..z</code> and <code>A..Z</code>.</p>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-none mb-8" id="description"><p>На этот раз без истории, без теории. Примеры ниже показывают, как написать функцию <code>accum</code>:</p>
<h4 id="examples">Примеры:</h4>
<pre><code>accum("abcd") ->"A-Bb-Ccc-Dddd"
accum("RqaEzty") ->"R-Qq-Aaa-Eeee-Zzzzz-Tttttt-Yyyyyyy"
accum("cwAt") ->"C-Ww-Aaa-Tttt"
</code></pre>
<p>Параметр функции accum — это строка, содержащая только буквы от <code>a..z</code> до <code>A..Z</code>.</p>
</div>