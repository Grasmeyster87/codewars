You are given an array with several "even" words, one "odd" word, and some numbers mixed in.

Determine if any of the numbers in the array is the index of the "odd" word. If so, return true, otherwise false.

Вам дан массив с несколькими «чётными» словами, одним «нечётным» словом и несколькими перемешанными числами.

Определите, является ли какое-либо из чисел в массиве индексом «нечётного» слова. Если да, верните true, иначе false.

Отличный вопрос 👍  
Смысл задания такой:

### 📝 Суть задачи
- У нас есть массив, в котором встречаются строки `"even"`, одна строка `"odd"`, и числа.  
- Нужно найти индекс слова `"odd"` в массиве.  
- Затем проверить: **есть ли в массиве число, равное этому индексу**.  
- Если да → вернуть `true`, иначе → `false`.

---

### 🔍 Пример разбора
```js
["even",4,"even",7,"even",55,"even",6,"even",10,"odd",3,"even"]
```
- Индекс `"odd"` = 10 (считаем с нуля).  
- В массиве есть число `10`.  
- Значит функция должна вернуть `true`.

Другой пример:
```js
["even",4,"even",7,"even",55,"even",6,"even",9,"odd",3,"even"]
```
- Индекс `"odd"` = 10.  
- В массиве чисел `10` нет.  
- Функция должна вернуть `false`.

---

### ✅ Решение
```js
function oddBall(arr) {
  // находим индекс слова "odd"
  const oddIndex = arr.indexOf("odd");
  // проверяем, есть ли такое число в массиве
  return arr.includes(oddIndex);
}
```

---

### 🔧 Объяснение кода
- `arr.indexOf("odd")` → возвращает позицию `"odd"` в массиве.  
- `arr.includes(oddIndex)` → проверяет, есть ли число, равное этому индексу.  
- Возвращаем результат проверки (`true` или `false`).  

---

Хочешь, я покажу, как это решение проходит твои тесты с `chai.assert`?
