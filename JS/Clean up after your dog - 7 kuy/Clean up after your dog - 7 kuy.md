<div class="markdown prose max-w-5xl mx-auto overflow-x-auto break-words" id="description"><p>You have stumbled across the divine pleasure that is owning a dog and a garden. Now time to pick up all the cr@p! :D</p>
<p>Given a 2D array to represent your garden, you must find and collect all of the dog cr@p - represented by <code>'@'</code>.</p>
<p>You will also be given the number of bags you have access to (<code>bags</code>), and the capactity of a bag (<code>cap</code>). If there are no <code>bags</code> then you can't pick anything up, so you can ignore <code>cap</code>.</p>
<p>You need to find out if you have enough capacity to collect all the cr@p and make your garden clean again. </p>
<p>If you do, return <code>'Clean'</code>, else return <code>'Cr@p'</code>.</p>
<p>Watch out though - if your dog is out there (<code>'D'</code>), he gets very touchy about being watched. If he is there you need to return <code>'Dog!!'</code>.</p>
<p>For example:</p>
<pre><code>bags = 2
cap = 2
x (or garden) =
[[ _ , _ , _ , _ , _ , _ ],
 [ _ , _ , _ , _ , @ , _ ],
 [ @ , _ , _ , _ , _ , _ ]]
</code></pre>
<p>returns <code>'Clean'</code></p>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-5xl mx-auto overflow-x-auto break-words" id="description"><p>Вы открыли для себя божественное удовольствие, которое дарит владение собакой и садом. Теперь пора убрать весь хлам! :D</p>
<p>Вам дан двумерный массив, представляющий ваш сад. Вы должны найти и собрать весь собачий хлам, обозначенный символом '@'</code>.</p>
<p>Вам также будет указано количество доступных вам мешков (<code>bags</code>) и вместимость мешка (<code>cap</code>). Если мешков нет, вы ничего не сможете собрать, поэтому можете игнорировать вместимость (<code>cap</code>).</p>
<p>Вам нужно выяснить, достаточно ли у вас вместимости, чтобы собрать весь хлам и снова привести свой сад в порядок. </p>
<p>Если вы это сделаете, верните <code>'Clean'</code>, иначе верните <code>'Cr@p'</code>.</p>
<p>Однако будьте осторожны — если ваша собака находится на улице (<code>'D'</code>), она очень чувствительна к тому, что за ней наблюдают. Если она там, вам нужно вернуть <code>'Dog!!'</code>.</p>
<p>Например:</p>
<pre><code>bags = 2
cap = 2
x (или garden) =
[[ _ , _ , _ , _ , _ , _ ],

[ _ , _ , _ , _ , @ , _ ],

[ @ , _ , _ , _ , _ , _ ]]
</code></pre>
<p>возвращает <code>'Clean'</code></p>
</div>