const chai = require('chai');
const expect = chai.expect;
const onlyOne = require('./Cuckoo Clock - 6 kuy');

describe("Tests", () => {
  it("test", () => {
assert.equal(onlyOne(true, false) == true, 'one true and one false')
  });
});
