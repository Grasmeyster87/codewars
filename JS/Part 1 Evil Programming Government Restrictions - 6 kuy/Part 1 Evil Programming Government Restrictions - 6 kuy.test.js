const { assert } = require('chai');
const anarchy = require('./Part 1 Evil Programming Government Restrictions - 6 kuy');

describe('Function', function () {
    it('Testing the function', function () {
        assert.strictEqual(anarchy(), 'I can write numbers like, 1, 2, 3.');
    });
});
