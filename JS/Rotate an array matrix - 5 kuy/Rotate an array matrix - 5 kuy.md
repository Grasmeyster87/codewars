<div class="markdown prose max-w-none mb-8" id="description"><p>Write a function that rotates a two-dimensional array (a matrix) either clockwise or anti-clockwise by 90 degrees, and  returns the rotated array.</p>
<p>The function accepts two parameters: a matrix, and a string specifying the direction or rotation. The direction will be either <code>"clockwise"</code> or <code>"counter-clockwise"</code>.</p>
<h2 id="examples">Examples</h2>
<p>For matrix:</p>
<pre><code>[
  [1, 2, 3],
  [4, 5, 6],
  [7, 8, 9]
]
</code></pre>
<p>Clockwise rotation:</p>
<pre><code>[
  [7, 4, 1],
  [8, 5, 2],
  [9, 6, 3]
]
</code></pre>
<p>Counter-clockwise rotation:</p>
<pre><code>[
  [3, 6, 9],
  [2, 5, 8],
  [1, 4, 7]
]
</code></pre>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-none mb-8" id="description"><p>Напишите функцию, которая поворачивает двумерный массив (матрицу) на 90 градусов либо по часовой стрелке, либо против часовой стрелки, и возвращает повернутый массив.</p>
<p>Функция принимает два параметра: матрицу и строку, указывающую направление поворота.</p> Направление будет либо по часовой стрелке</code>, либо против часовой стрелки</code>.
<h2 id="examples">Примеры</h2>
<p>Для матрицы:</p>
<pre><code>[
[1, 2, 3],

[4, 5, 6],

[7, 8, 9]
]
</code></pre>

<p>Вращение по часовой стрелке:</p>
<pre><code>[

[7, 4, 1],

[8, 5, 2],

[9, 6, 3]
]
</code></pre>

<p>Вращение против часовой стрелки:</p>
<pre><code>[

[3, 6, 9],

[2, 5, 8],

[1, 4, 7]
]
</code></pre>

</div>

Добре 👍  
Ось **логіка розв’язання задачі без коду**:

---

## 🔄 Загальна ідея

Ми маємо матрицю (двовимірний масив) і потрібно повернути її на 90°:

-   **за годинниковою стрілкою (clockwise)**
-   **проти годинникової стрілки (counter-clockwise)**

При повороті змінюється кількість рядків і стовпців:

-   Якщо матриця була `m × n`, то після повороту вона стане `n × m`.

---

## 🧩 Кроки для повороту **за годинниковою стрілкою**

1. Взяти перший стовпець матриці і зробити його першим рядком у новій матриці, але в зворотному порядку.
2. Повторити для кожного стовпця:
    - другий стовпець стає другим рядком, теж у зворотному порядку.
    - і так далі.
3. У результаті верхній рядок нової матриці складається з останнього стовпця старої матриці.

👉 Іншими словами: **рядки нової матриці = стовпці старої матриці, але розташовані знизу догори.**

---

## 🧩 Кроки для повороту **проти годинникової стрілки**

1. Взяти останній стовпець матриці і зробити його першим рядком у новій матриці (без зміни порядку).
2. Потім взяти передостанній стовпець і зробити другим рядком.
3. Продовжити для всіх стовпців.
4. У результаті верхній рядок нової матриці складається з першого стовпця старої матриці.

👉 Іншими словами: **рядки нової матриці = стовпці старої матриці, але розташовані зверху донизу.**

---

## 📐 Приклад

Матриця:

```
[
  [1, 2, 3],
  [4, 5, 6],
  [7, 8, 9]
]
```

-   **Clockwise**: беремо стовпці справа наліво →

    ```
    [7,4,1]
    [8,5,2]
    [9,6,3]
    ```

-   **Counter-clockwise**: беремо стовпці зліва направо →
    ```
    [3,6,9]
    [2,5,8]
    [1,4,7]
    ```

---

## 🧠 Важливі моменти

-   Якщо матриця квадратна (однакова кількість рядків і стовпців), вона просто повертається.
-   Якщо прямокутна (наприклад, 4×3), то після повороту розміри змінюються (3×4).
-   Якщо матриця має лише один рядок або один стовпець, вона перетворюється на вертикальний або горизонтальний масив.
-   Якщо матриця 1×1 — вона не змінюється.

---

Хочеш, я поясню ще **візуально на схемі** (наприклад, як стрілки показують рух елементів при повороті)?
