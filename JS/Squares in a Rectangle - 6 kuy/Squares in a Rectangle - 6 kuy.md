<div class="markdown prose max-w-none mb-8" id="description"><p>A rectangle can be split up into a grid of 1x1 squares, the amount of which being equal to the product of the two dimensions of the rectangle. Depending on the size of the rectangle, that grid of 1x1 squares can also be split up into larger squares, for example a 3x2 rectangle has a total of 8 squares, as there are 6 distinct 1x1 squares, and two possible 2x2 squares. A 4x3 rectangle contains 20 squares.</p>
<p>Your task is to write a function that returns the total number of squares for any given rectangle, the dimensions of which being given as two integers with the first always being equal to or greater than the second.</p>
</div>

<br><hr><br>

<div class="markdown prose max-w-none mb-8" id="description"><p>Прямоугольник можно разбить на сетку из квадратов 1x1, количество которых равно произведению двух размеров прямоугольника. В зависимости от размера прямоугольника, эта сетка из квадратов 1x1 также может быть разделена на квадраты большего размера. Например, прямоугольник 3x2 содержит в общей сложности 8 квадратов, так как существует 6 различных квадратов 1x1 и два возможных квадрата 2x2. Прямоугольник 4x3 содержит 20 квадратов.</p>
<p>Ваша задача — написать функцию, которая возвращает общее количество квадратов для любого заданного прямоугольника, размеры которого заданы двумя целыми числами, причем первое всегда равно или больше второго.</p>
</div>


Добре, поясню простими словами 🙂  

### 🔑 Чому саме **+1**
У формулі  
\[
(x - k + 1) \cdot (y - k + 1)
\]  
це **+1** з’являється через те, що ми рахуємо кількість можливих позицій для квадрата розміру `k × k`.

---

### 📐 Приклад без +1
Уявімо прямокутник **4 × 3** і квадрат **2 × 2**:

- По ширині (x = 4):  
  Якщо відняти просто `x - k = 4 - 2 = 2`, то ми отримаємо лише "зсуви", але не врахуємо першу позицію.  
  Насправді квадрат можна поставити у **3 різних місцях**:  
  - починаючи з колонки 1,  
  - починаючи з колонки 2,  
  - починаючи з колонки 3.  

  Тому треба додати ще одну позицію → `x - k + 1 = 3`.

- По висоті (y = 3):  
  Аналогічно, квадрат 2×2 можна поставити у **2 місцях**:  
  - починаючи з рядка 1,  
  - починаючи з рядка 2.  

  Формула дає `y - k + 1 = 2`.

---

### 🧮 Загальна логіка
- Якщо квадрат має розмір `k`, то він займає `k` клітинок.  
- Кількість можливих стартових позицій уздовж осі = (довжина прямокутника - довжина квадрата) **+ 1**.  
  Це "одиниця" якраз і враховує першу можливу позицію.

---

### 📌 Візуалізація
Для прямокутника 4×3 і квадрата 2×2:

```
[1][2][3][4]   ← можливі стартові колонки
 ^  ^  ^
```

Тут видно, що стартових позицій **3**, а не 2. Саме тому потрібне **+1**.

---

Отже, **+1** — це поправка, щоб врахувати першу позицію розміщення квадрата.  
Хочеш, я намалюю ASCII-схему для різних розмірів квадратів, щоб було ще наочніше?
