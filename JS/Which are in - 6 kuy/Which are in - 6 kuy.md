<div class="markdown prose max-w-none mb-8" id="description"><p>Given two arrays of strings <code>a1</code> and <code>a2</code> return a sorted array <code>r</code> in lexicographical order of the strings of <code>a1</code> which are substrings of strings of <code>a2</code>.</p>
<h4 id="example-1">Example 1:</h4>
<p><code>a1 = ["arp", "live", "strong"]</code></p>
<p><code>a2 = ["lively", "alive", "harp", "sharp", "armstrong"]</code></p>
<p>returns <code>["arp", "live", "strong"]</code></p>
<h4 id="example-2">Example 2:</h4>
<p><code>a1 = ["tarp", "mice", "bull"]</code></p>
<p><code>a2 = ["lively", "alive", "harp", "sharp", "armstrong"]</code></p>
<p>returns <code>[]</code></p>
<h4 id="notes">Notes:</h4>
<ul>
<li>Arrays are written in "general" notation. See "Your Test Cases" for examples in your language.</li>
<li>In Shell bash <code>a1</code> and <code>a2</code> are strings. The return is a string where words are separated by commas.</li>
<li>Beware: In some languages <code>r</code> must be without duplicates.</li>
</ul>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-none mb-8" id="description"><p>Дано два масиви рядків <code>a1</code> та <code>a2</code>, які повертають відсортований масив <code>r</code> у лексикографічному порядку рядків <code>a1</code>, які є підрядками рядків <code>a2</code>.</p>
<h4 id="example-1">Приклад 1:</h4>
<p><code>a1 = ["arp", "live", "strong"]</code></p>
<p><code>a2 = ["lively", "alive", "harp", "sharp", "armstrong"]</code></p>
<p>повертає <code>["arp", "live", "strong"]</code></p>
<h4 id="example-2">Приклад 2:</h4>
<p><code>a1 = ["tarp", "mice", "bull"]</code></p>
<p><code>a2 = ["lively", "alive", "harp", "sharp", "armstrong"]</code></p>
<p>повертає <code>[]</code></p>
<h4 id="notes">Примітки:</h4>
<ul>
<li>Масиви записуються в "загальній" нотації. Дивіться "Ваші тестові випадки" для прикладів вашою мовою.</li>
<li>У Shell bash <code>a1</code> та <code>a2</code> є рядками. Поверненням є рядок, де слова розділені комами.</li>
<li>Увага: У деяких мовах <code>r</code> має бути без дублікатів.</li>
</ul>
</div>