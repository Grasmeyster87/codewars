<div class="markdown prose max-w-none mb-8" id="description"><p>You are given an <em>odd-length</em> array of integers, in which all of them are the same, except for one single number.</p>
<p>Complete the method which accepts such an array, and returns that single different number.</p>
<p><strong>The input array will always be valid!</strong> (odd-length &gt;= 3)</p>
<h2 id="examples">Examples</h2>
<pre><code>[1, 1, 2] ==&gt; 2
[17, 17, 3, 17, 17, 17, 17] ==&gt; 3
</code></pre>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-none mb-8" id="description"><p>Вам дан массив целых чисел нечетной длины, в котором все числа одинаковы, за исключением одного единственного числа.</p>
<p>Завершите метод, который принимает такой массив и возвращает это единственное отличающееся число.</p>
<p><strong>Входной массив всегда будет действителен!</strong> (нечетная длина >= 3)</p>
<h2 id="examples">Примеры</h2>
<pre><code>[1, 1, 2] ==>2
[17, 17, 3, 17, 17, 17, 17] ==>3
</code></pre>
</div>