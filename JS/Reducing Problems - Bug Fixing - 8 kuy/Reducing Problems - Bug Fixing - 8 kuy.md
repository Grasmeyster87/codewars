<div class="markdown prose max-w-none mb-8" id="description"><p>Oh no! Timmy's <code>reduce</code> is causing problems, Timmy's goal is to calculate the two teams scores and return the winner but timmy has gotten confused and sometimes teams don't enter their scores, total the scores out of 3! Help Timmy fix his program!</p>
<p>Return <code>true</code> if team 1 wins or <code>false</code> if team 2 wins!</p>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-none mb-8" id="description"><p>О нет! Программа Тимми <code>reduce</code> создает проблемы. Цель Тимми — подсчитать очки двух команд и определить победителя, но Тимми запутался, и иногда команды не вводят свои очки, а суммируют их по 3-балльной шкале! Помогите Тимми исправить программу!</p>
<p>Вернуть <code>true</code>, если побеждает команда 1, или <code>false</code>, если побеждает команда 2!</p>
</div>
