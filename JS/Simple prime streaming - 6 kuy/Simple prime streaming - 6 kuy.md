<div class="markdown prose max-w-none mb-8" id="description"><p>Consider a sequence made up of the consecutive prime numbers. This infinite sequence would start with: </p>
<pre><code class="language-python"><span class="cm-string">"2357111317192329313741434753596167717379..."</span>
</code></pre>
<p>You will be given two numbers: <code>a</code> and <code>b</code>, and your task will be to return <code>b</code> elements starting from index <code>a</code> in this sequence.</p>
<pre><code>For example:
solve(10,5) == `19232` Because these are 5 elements from index 10 in the sequence.
</code></pre>
<p>Tests go up to about index <code>20000</code>.</p>
<p>More examples in test cases. Good luck!</p>
<p>Please also try <a href="https://www.codewars.com/kata/5b76a34ff71e5de9db0000f2" data-turbolinks="false" target="_blank">Simple time difference</a></p>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-none mb-8" id="description"><p>Рассмотрим последовательность, состоящую из последовательных простых чисел. Эта бесконечная последовательность будет начинаться с: </p>
<pre><code class="language-python"><span class="cm-string">"2357111317192329313741434753596167717379..."</span>
</code></pre>
<p>Вам будут даны два числа: <code>a</code> и <code>b</code>, и ваша задача будет заключаться в том, чтобы вернуть <code>b</code> элементов, начиная с индекса <code>a</code> в этой последовательности.</p>
<pre><code>Например:
solve(10,5) == `19232`, потому что это 5 элементов с индекса 10 в последовательности.

<pre><code>Например:
solve(10,5) == `19232`, потому что это 5 элементов с индекса 10 в последовательности.

</code></pre>
<p>Тесты доходят примерно до индекса <code>20000</code>.</p>
<p>Больше примеров в тестовых случаях. Удачи!</p>
<p>Пожалуйста, попробуйте также <a href="https://www.codewars.com/kata/5b76a34ff71e5de9db0000f2" data-turbolinks="false" target="_blank">Простая разница во времени</a></p>
</div>
