<div class="markdown prose max-w-5xl mx-auto overflow-x-auto break-words" id="description"><p>You are given a secret message you need to decipher. Here are the things you need to know to decipher it:</p>
<p>For each word:</p>
<ul>
<li>the second and the last letter is switched (e.g. <code>Hello</code> becomes <code>Holle</code>)</li>
<li>the first letter is replaced by its character code (e.g. <code>H</code> becomes <code>72</code>)</li>
</ul>
<ul>
<li>there are no special characters used, only letters and spaces</li>
<li>words are separated by a single space</li>
<li>there are no leading or trailing spaces</li>
</ul>
<p>Examples</p>
<pre><code>'72olle 103doo 100ya' --&gt; 'Hello good day'
'82yade 115te 103o'   --&gt; 'Ready set go'
</code></pre>
</div>

<br> <hr> <br>

<div class="markdown prose max-w-5xl mx-auto overflow-x-auto break-words" id="description"><p>You are given a secret message you need to decipher. Here are the things you need to know to decipher it:</p>
<p>For each word:</p>
<ul>
<li>the second and the last letter is switched (e.g. <code>Hello</code> becomes <code>Holle</code>)</li>
<li>the first letter is replaced by its character code (e.g. <code>H</code> becomes <code>72</code>)</li>
</ul>
<ul>
<li>there are no special characters used, only letters and spaces</li>
<li>words are separated by a single space</li>
<li>there are no leading or trailing spaces</li>
</ul>
<p>Examples</p>
<pre><code>'72olle 103doo 100ya' --&gt;
763
<div class="markdown prose max-w-5xl mx-auto overflow-x-auto break-words" id="description"><p>Вам передано секретное сообщение, которое необходимо расшифровать. Вот что вам нужно знать, чтобы расшифровать это:</p>
<p>Для каждого слова:</p>
<ul>
<li>вторая и последняя буква меняются местами (например, <code>Hello</code> становится <code>Holle</code>)</li>
<li>первая буква заменяется ее кодом символа (например, <code>H</code> становится <code>72</code>)</li>
</ul>
<ul>
<li>специальные символы не используются, только буквы и пробелы</li>
<li>слова разделены одним пробелом</li>
<li>пробелов в начале и конце нет</li>
</ul>
<p>Примеры</p>
<pre><code>'72olle 103doo 100ya' --&gt; 'Hello good day'
'82yade 115te 103o' --&gt; «На старт, внимание, марш!»
</code></pre>
</div>


Добре, давай розберемо функцію **`decipherThis`** крок за кроком, щоб було зрозуміло, як вона працює і чому дає правильний результат.

---

## 🔎 Алгоритм роботи

### 1. Розбиття рядка на слова
```js
str.split(' ')
```
- Вхідний рядок складається з кількох закодованих слів, розділених пробілом.
- Ми розбиваємо його на масив слів, щоб обробляти кожне окремо.

---

### 2. Витягування числового коду
```js
const codeMatch = word.match(/^\d+/);
const charCode = codeMatch ? String.fromCharCode(codeMatch[0]) : '';
```
- `word.match(/^\d+/)` знаходить усі цифри на початку слова (це ASCII‑код першої літери).
- Наприклад: `"72olle"` → `"72"`.
- `String.fromCharCode(72)` → `"H"`.
- Це і є перша літера розшифрованого слова.

---

### 3. Залишок слова
```js
let rest = word.slice(codeMatch[0].length);
```
- Вирізаємо частину слова після цифр.
- `"72olle"` → залишок `"olle"`.

---

### 4. Перестановка літер
```js
if (rest.length > 1) {
  let chars = rest.split('');
  [chars[0], chars[chars.length - 1]] = [chars[chars.length - 1], chars[0]];
  rest = chars.join('');
}
```
- Якщо залишок має більше ніж 1 символ:
  - міняємо місцями **першу і останню літеру залишку**.
- `"olle"` → `"ello"`.
- `"doo"` → `"ood"`.
- `"ya"` → `"ay"`.

---

### 5. Формування слова
```js
return charCode + rest;
```
- Склеюємо першу літеру (з коду) + оброблений залишок.
- `"H" + "ello"` → `"Hello"`.

---

### 6. Збирання всього рядка
```js
.join(' ')
```
- Після обробки кожного слова ми збираємо їх назад у рядок через пробіл.

---

## 📌 Приклад роботи

Вхід:
```js
"72olle 103doo 100ya"
```

Кроки:
1. `"72olle"` → код `72` → `"H"`, залишок `"olle"` → `"ello"` → `"Hello"`.
2. `"103doo"` → код `103` → `"g"`, залишок `"doo"` → `"ood"` → `"good"`.
3. `"100ya"` → код `100` → `"d"`, залишок `"ya"` → `"ay"` → `"day"`.

Вихід:
```js
"Hello good day"
```

---

## ✅ Підсумок
Функція:
- розбиває рядок на слова,
- дістає ASCII‑код першої літери,
- міняє місцями перший і останній символ залишку,
- збирає результат назад у рядок.

Таким чином вона правильно розшифровує повідомлення згідно з правилами.  

Хочеш, я зроблю схему‑діаграму (таблицю) для ще одного прикладу, наприклад `"82yade"` → `"Ready"`?



Добре 👍, давай зробимо детальну схему‑таблицю для прикладу **`82yade` → `Ready`**.  

---

## 📊 Таблиця розшифрування слова `82yade`

| Крок | Дія | Результат |
|------|-----|-----------|
| 1 | Витягуємо числовий код на початку слова: `82` | Це ASCII‑код літери **`R`** |
| 2 | Залишок слова після цифр: `"yade"` | Маємо рядок `"yade"` |
| 3 | Міняємо місцями **першу і останню літеру** залишку | `"yade"` → `"eady"` |
| 4 | Склеюємо першу літеру з коду + оброблений залишок | `"R" + "eady"` → **`Ready`** |

---

## 🔎 Візуалізація процесу
```
Закодоване слово: 82yade
   ↓
ASCII(82) = "R"
   ↓
Залишок = "yade"
   ↓
Перестановка: "yade" → "eady"
   ↓
Результат: "Ready"
```

---

Таким чином, функція працює так само для кожного слова:  
- бере код → перетворює у літеру,  
- переставляє перший і останній символ залишку,  
- збирає все разом.  

Хочеш, я зроблю таку ж таблицю для цілого рядка `"82yade 115te 103o"` → `"Ready set go"`?
